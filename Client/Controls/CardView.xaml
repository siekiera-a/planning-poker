<UserControl x:Class="Client.Controls.CardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Client.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             FontSize="50">
    <Border BorderThickness="3" CornerRadius="10" Background="{StaticResource MahApps.Brushes.Button.Border.MouseOver}">
        <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" Name="CardValue" Text="{Binding Card.Name}"/>

    </Border>
</UserControl>